# Asks and Question FORUM

# To start and run 

Clone the repository and use ` npm install ` to download the dependencies.

Next use `npm run dev ` to start the dev server or

use `npm run server ` to start the application
